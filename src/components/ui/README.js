/**
 * components/ui/ — shadcn/ui primitives (Base UI + CVA)
 *
 * Cài thêm: npx shadcn@latest add <component> -y
 *
 * Feature/page import từ @/components/admin — KHÔNG import trực tiếp ui/*.
 */

export {};
